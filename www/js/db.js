var ddl = "CREATE TABLE IF NOT EXISTS country ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT 	,label VARCHAR(50) NOT NULL UNIQUE );  CREATE TABLE IF NOT EXISTS city ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT 	,label VARCHAR(50) NOT NULL  	,country_id INTEGER NOT NULL REFERENCES country (id) );   CREATE TABLE IF NOT EXISTS school ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT 	,label VARCHAR(50) NOT NULL  	,city_id INTEGER NOT NULL REFERENCES city (id) );    CREATE TABLE IF NOT EXISTS class ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT  	,label VARCHAR(50) NOT NULL UNIQUE  	,school_id INTEGER NOT NULL REFERENCES school (id)  );    CREATE TABLE IF NOT EXISTS person ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT  	,given VARCHAR(50)    	,family VARCHAR(50)    	,sex TINYINT    	,email VARCHAR(50)    	,phone VARCHAR(20)    	,is_auth TINYINT NOT NULL  	,access_code VARCHAR(10)   	,role VARCHAR(10) NOT NULL  );   CREATE TABLE IF NOT EXISTS parent ( 	 parent_id INTEGER NOT NULL REFERENCES person (id)  	,child_id INTEGER NOT NULL REFERENCES person (id)  );   CREATE TABLE IF NOT EXISTS invite ( 	 from_id INTEGER NOT NULL REFERENCES person (id)  	,to_id INTEGER NOT NULL REFERENCES person (id)  	,sent_at TEXT NOT NULL  );    CREATE TABLE IF NOT EXISTS member ( 	 class_id INTEGER NOT NULL REFERENCES class (id)  	,person_id INTEGER NOT NULL REFERENCES person (id)  );   CREATE TABLE IF NOT EXISTS homework ( 	 id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT 	,title VARCHAR(50) NOT NULL  	,task VARCHAR(250)    	,is_private TINYINT    	,due TEXT DEFAULT CURRENT_TIMESTAMP  	,creator_id INTEGER NOT NULL REFERENCES person (id)    	,class_id INTEGER NOT NULL REFERENCES class (id)  );  CREATE TABLE IF NOT EXISTS homework_personal ( 	 creator_id INTEGER NOT NULL REFERENCES person (id)    	,homework_id INTEGER NOT NULL REFERENCES homework (id)  	,my_progress INTEGER NOT NULL DEFAULT 0 	,my_notes VARCHAR(250)    ); ";